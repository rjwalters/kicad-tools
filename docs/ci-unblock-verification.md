# CI unblock verification

This temporary documentation-only change verifies that the content-contract
step added by PR #5250 runs while unrelated board jobs are skipped.

The verification PR will be closed without merging after the CI evidence is
recorded on issue #5249.
